const React = require('react');

module.exports = React.createClass({
    propTypes: {
    },

    render() {
        return (
            <div>
                hello
            </div>
        )
    }
});
